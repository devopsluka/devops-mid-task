# ============================================================================
# Environment Variables for golang-webapp.devops-mid-task.com
# ============================================================================
# Copy this file to .env and customize as needed:
#   cp .env.example .env
#
# Docker Compose will automatically load variables from .env file
# ============================================================================

# ----------------------------------------------------------------------------
# Application Configuration
# ----------------------------------------------------------------------------

# API Version displayed in health endpoint
API_VERSION=1.0.0

# Backend HTTPS port (internal)
WEBAPP_HTTPS_PORT=8443

# ----------------------------------------------------------------------------
# Nginx Configuration
# ----------------------------------------------------------------------------

# HTTP port (external) - redirects to HTTPS
# For production: 80 (requires root/privileges)
# For development: 8080 (unprivileged)
HTTP_PORT=80

# HTTPS port (external) - SSL/TLS termination
# For production: 443 (requires root/privileges)
# For development: 8443 (unprivileged)
HTTPS_PORT=443

# ----------------------------------------------------------------------------
# SSL/TLS Certificates
# ----------------------------------------------------------------------------

# Certificate paths (inside webapp container)
TLS_CERT_FILE=/root/certs/server.crt
TLS_KEY_FILE=/root/certs/server.key

# ----------------------------------------------------------------------------
# Docker Compose Settings
# ----------------------------------------------------------------------------

# Compose project name
COMPOSE_PROJECT_NAME=devops-mid-task

# ----------------------------------------------------------------------------
# Optional: Resource Limits (uncomment in docker-compose.yml)
# ----------------------------------------------------------------------------

# Webapp resource limits
# WEBAPP_CPU_LIMIT=1.0
# WEBAPP_MEMORY_LIMIT=512M
# WEBAPP_CPU_RESERVATION=0.5
# WEBAPP_MEMORY_RESERVATION=256M

# Nginx resource limits
# NGINX_CPU_LIMIT=0.5
# NGINX_MEMORY_LIMIT=256M
# NGINX_CPU_RESERVATION=0.25
# NGINX_MEMORY_RESERVATION=128M

# ----------------------------------------------------------------------------
# Development vs Production
# ----------------------------------------------------------------------------

# Environment type (development, production)
ENVIRONMENT=production

# Enable debug logging
# DEBUG=false
